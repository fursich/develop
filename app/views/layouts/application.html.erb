<!DOCTYPE html>
<html lang="ja">
  <head>
    <!-- <title>SolidusRails5</title> -->
    <title><%= yield(:title).present? ? "#{yield(:title)} - " : "" %>BIGBAG Store</title>

    <%= csrf_meta_tags %>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- PLUGINS CSS STYLE -->

    <!-- GOOGLE FONT -->
    <%= stylesheet_link_tag "application", 'https://fonts.googleapis.com/css?family=Oxygen:400,300,700', :media => "all" %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <!-- CUSTOM CSS -->

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>

  <body>

    <div class="main-wrapper">
      <!-- HEADER -->
      <div class="header clearfix">
        <!-- TOPBAR -->
        <%= render partial: 'potepan/partial/topbar' %>
        <!-- NAVBAR -->
        <%= render partial: 'potepan/partial/navbar' %>
      </div>

      <%= yield %>
      <!-- LIGHT SECTION -->
      <%= render partial: 'potepan/partial/partners' %>

      <!-- FOOTER -->
      <!-- COPY RIGHT -->
      <%= render partial: 'potepan/partial/footer' %>
    </div>
    
    <!-- LOGIN MODAL -->
    <!-- SIGN UP MODAL -->
    <%= render partial: 'potepan/partial/auth_modals' %>
  </body>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

</html>
